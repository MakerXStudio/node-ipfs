// import { S3ObjectCache, PinataStorageWithCache } from '../src'
// import { S3 } from '@aws-sdk/client-s3'

describe('ipfs', () => {
  it('can put a blob to ipfs', async () => {
    expect(1).toBe(1)
    // const s3Client = new S3({
    //   region: process.env.AWS_REGION,
    //   // endpoint: AWS_ENDPOINT,
    // })
    // const cache = new S3ObjectCache(s3Client, 'bucker', 'cache/ipfs/') // Mock s3
    // const ipfs = new PinataStorageWithCache('', cache) // Mock the HTTP calls
    // const blob = new Uint8Array([1, 2, 3])
    // const { cid } = await ipfs.putBlob(blob, '', 'test')
  })
})
